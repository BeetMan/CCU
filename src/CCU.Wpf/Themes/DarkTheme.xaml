<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ================================================================
         Dark Theme — 机械师品牌风格 "Obsidian Neon"
         暗黑基底 + 青蓝霓虹点缀 + 玻璃拟态层
         ================================================================ -->

    <!-- ── Core palette ── -->
    <Color x:Key="BgPrimary">#FF0B0E14</Color>
    <Color x:Key="BgSecondary">#FF11151E</Color>
    <Color x:Key="BgTertiary">#FF181C28</Color>
    <Color x:Key="BgCard">#1AFFFFFF</Color>
    <Color x:Key="BgGlass">#0DFFFFFF</Color>
    <Color x:Key="BgGlassHover">#20FFFFFF</Color>
    <Color x:Key="BgActive">#1E00D4AA</Color>

    <Color x:Key="AccentPrimary">#FF00D4AA</Color>
    <Color x:Key="AccentSecondary">#FF0EA5E9</Color>
    <Color x:Key="AccentWarn">#FFF59E0B</Color>
    <Color x:Key="AccentDanger">#FFEF4444</Color>
    <Color x:Key="AccentTurbo">#FFF97316</Color>

    <Color x:Key="TextPrimary">#FFF1F5F9</Color>
    <Color x:Key="TextSecondary">#FF94A3B8</Color>
    <Color x:Key="TextMuted">#FF475569</Color>
    <Color x:Key="TextAccent">#FF00D4AA</Color>

    <Color x:Key="BorderSubtle">#1AFFFFFF</Color>
    <Color x:Key="BorderCard">#22FFFFFF</Color>
    <Color x:Key="BorderAccent">#3300D4AA</Color>

    <!-- ── Brushes ── -->
    <SolidColorBrush x:Key="BrushBgPrimary" Color="{StaticResource BgPrimary}" />
    <SolidColorBrush x:Key="BrushBgSecondary" Color="{StaticResource BgSecondary}" />
    <SolidColorBrush x:Key="BrushBgTertiary" Color="{StaticResource BgTertiary}" />
    <SolidColorBrush x:Key="BrushBgCard" Color="{StaticResource BgCard}" />
    <SolidColorBrush x:Key="BrushBgGlass" Color="{StaticResource BgGlass}" />
    <SolidColorBrush x:Key="BrushBgGlassHover" Color="{StaticResource BgGlassHover}" />
    <SolidColorBrush x:Key="BrushBgActive" Color="{StaticResource BgActive}" />

    <SolidColorBrush x:Key="BrushAccentPrimary" Color="{StaticResource AccentPrimary}" />
    <SolidColorBrush x:Key="BrushAccentSecondary" Color="{StaticResource AccentSecondary}" />
    <SolidColorBrush x:Key="BrushAccentWarn" Color="{StaticResource AccentWarn}" />
    <SolidColorBrush x:Key="BrushAccentDanger" Color="{StaticResource AccentDanger}" />
    <SolidColorBrush x:Key="BrushAccentTurbo" Color="{StaticResource AccentTurbo}" />

    <SolidColorBrush x:Key="BrushTextPrimary" Color="{StaticResource TextPrimary}" />
    <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource TextSecondary}" />
    <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource TextMuted}" />
    <SolidColorBrush x:Key="BrushTextAccent" Color="{StaticResource TextAccent}" />

    <SolidColorBrush x:Key="BrushBorderSubtle" Color="{StaticResource BorderSubtle}" />
    <SolidColorBrush x:Key="BrushBorderCard" Color="{StaticResource BorderCard}" />
    <SolidColorBrush x:Key="BrushBorderAccent" Color="{StaticResource BorderAccent}" />

    <!-- ── Gradient brushes ── -->
    <LinearGradientBrush x:Key="GradientAccent" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource AccentPrimary}" Offset="0" />
        <GradientStop Color="{StaticResource AccentSecondary}" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="GradientAccentVertical" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource AccentPrimary}" Offset="0" />
        <GradientStop Color="{StaticResource AccentSecondary}" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="GradientBgHeader" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#FF0B0E14" Offset="0" />
        <GradientStop Color="#FF111827" Offset="0.5" />
        <GradientStop Color="#FF0B0E14" Offset="1" />
    </LinearGradientBrush>

    <!-- ── Typography ── -->
    <FontFamily x:Key="FontDisplay">Segoe UI</FontFamily>
    <FontFamily x:Key="FontBody">Segoe UI</FontFamily>
    <FontFamily x:Key="FontMono">Cascadia Code, Consolas, Courier New</FontFamily>

    <Style x:Key="TextDisplayLarge" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource FontDisplay}" />
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
    </Style>
    <Style x:Key="TextHeading" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource FontDisplay}" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
    </Style>
    <Style x:Key="TextSubtitle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource FontBody}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}" />
    </Style>
    <Style x:Key="TextBody" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource FontBody}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
    </Style>
    <Style x:Key="TextCaption" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource FontBody}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}" />
    </Style>
    <Style x:Key="TextMono" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource FontMono}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextAccent}" />
    </Style>

    <!-- ── Glass card style ── -->
    <Style x:Key="GlassCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BrushBgCard}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorderCard}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="12" />
    </Style>

    <!-- ── Nav button style ── -->
    <Style x:Key="NavButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Padding" Value="16,10" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BrushBgGlassHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Accent button ── -->
    <Style x:Key="AccentButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GradientAccent}" />
        <Setter Property="Foreground" Value="#FF0B0E14" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Padding" Value="20,10" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.88" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.72" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Ghost button ── -->
    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource BrushAccentPrimary}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorderAccent}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BrushBgGlassHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Icon button (compact ghost) ── -->
    <Style x:Key="IconButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Width" Value="36" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            CornerRadius="6" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushBgGlassHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Toggle Switch ── -->
    <Style x:Key="ToggleSwitch" TargetType="CheckBox">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="switchRoot" Width="44" Height="24" CornerRadius="12"
                            Background="{StaticResource BrushBgTertiary}"
                            BorderBrush="{StaticResource BrushBorderSubtle}" BorderThickness="1">
                        <Ellipse x:Name="thumb" Width="18" Height="18" Fill="{StaticResource BrushTextSecondary}"
                                 HorizontalAlignment="Left" Margin="2,0,0,0" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="switchRoot" Property="Background" Value="{StaticResource BrushAccentPrimary}" />
                            <Setter TargetName="switchRoot" Property="BorderBrush" Value="{StaticResource BrushAccentPrimary}" />
                            <Setter TargetName="thumb" Property="Fill" Value="#FF0B0E14" />
                            <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right" />
                            <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Slider (neon) ── -->
    <Style x:Key="NeonSlider" TargetType="Slider">
        <Setter Property="Height" Value="24" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <Border x:Name="trackBg" Height="4" CornerRadius="2"
                                Background="{StaticResource BrushBgTertiary}" VerticalAlignment="Center" />
                        <Border x:Name="trackFill" Height="4" CornerRadius="2"
                                Background="{StaticResource GradientAccent}" VerticalAlignment="Center"
                                HorizontalAlignment="Left" />
                        <Track x:Name="PART_Track">
                            <Track.Thumb>
                                <Thumb Width="16" Height="16" Cursor="Hand">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Ellipse Fill="{StaticResource BrushAccentPrimary}" Width="16" Height="16">
                                                <Ellipse.Effect>
                                                    <DropShadowEffect Color="{StaticResource AccentPrimary}"
                                                                      BlurRadius="8" ShadowDepth="0" Opacity="0.6" />
                                                </Ellipse.Effect>
                                            </Ellipse>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
