<UserControl x:Class="CCU.Wpf.Views.FanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:CCU.Wpf.Converters"
             xmlns:vm="clr-namespace:CCU.Wpf.ViewModels">

    <StackPanel x:Name="LayoutRoot">
        <TextBlock Text="风扇控制" Style="{StaticResource TextHeading}" />
        <TextBlock Text="拖拽节点调节 CPU / GPU 散热曲线" Style="{StaticResource TextSubtitle}" Margin="0,4,0,24" />

        <!-- 模式切换 -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <Button Style="{StaticResource GhostButton}" Content="自动" Margin="0,0,8,0"
                    Command="{Binding SetModeCommand}" CommandParameter="auto" />
            <Button Style="{StaticResource GhostButton}" Content="手动" Margin="0,0,8,0"
                    Command="{Binding SetModeCommand}" CommandParameter="manual" />
            <Button Style="{StaticResource GhostButton}" Content="全速"
                    Command="{Binding SetModeCommand}" CommandParameter="max" />
            <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding IndependentControl}"
                      Margin="24,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="风扇独立控制" FontSize="12" Foreground="{StaticResource BrushTextSecondary}"
                       VerticalAlignment="Center" Margin="8,0,0,0" />
        </StackPanel>

        <!-- CPU 风扇曲线 -->
        <TextBlock Text="CPU 风扇曲线" Style="{StaticResource SectionHeader}" />
        <conv:FanCurvePanel CurveData="{Binding CpuCurve}" LineColor="#FF00D4AA" FillColor="#2200D4AA" />

        <TextBlock Text=" " Height="20" />

        <!-- GPU 风扇曲线 -->
        <TextBlock Text="GPU 风扇曲线" Style="{StaticResource SectionHeader}" />
        <conv:FanCurvePanel CurveData="{Binding GpuCurve}" LineColor="#FF0EA5E9" FillColor="#220EA5E9" />
    </StackPanel>
</UserControl>
